app:
  name: "mm-bot"
  log_level: "INFO"
  tick_seconds: 1

risk:
  max_total_notional: 2000
  daily_loss_limit: 25
  max_inventory_notional: 300

venues:
  extended:
    enabled: true
  nado:
    enabled: true

extended_ws:
  host: "wss://api.starknet.extended.exchange"
  market: "BTC-USD"
  depth: 1
  user_agent: "mm-bot/0.1"

extended:
  markets: ["BTC-USD", "ETH-USD", "SOL-USD"]
  selector:
    min_spread_bps: 0.08
    min_tpm: 5
    top_n: 2

sim:
  enabled: true
  quote_half_spread_bps: 0.10
  quote_size_usd: 25
  max_inventory_usd: 150
